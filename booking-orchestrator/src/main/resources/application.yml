spring:
  rabbitmq:
    host: dog.lmq.cloudamqp.com
    port: 5672
    username: ixejxqrs
    password: your-lavinmq-password
    virtual-host: ixejxqrs
    template:
      exchange: orchestrator.exchange
    listener:
      simple:
        prefetch: 1
        concurrency: 3
        max-concurrency: 10
        retry:
          enabled: true
          initial-interval: 2000ms
          max-attempts: 3
          max-interval: 10000ms
          multiplier: 2.0
    logging:
      level:
        org.springframework.amqp: DEBUG
